import { Meta } from '@storybook/react';
import { FooterView } from './Footer.view';

export default {
  title: 'Organism/Footer',
  component: FooterView,
} as Meta;

export const Default = () => <FooterView />;
