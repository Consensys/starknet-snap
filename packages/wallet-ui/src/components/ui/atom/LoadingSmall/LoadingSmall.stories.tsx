import { Meta } from '@storybook/react';
import { LoadingSmallView } from './LoadingSmall.view';

export default {
  title: 'Atom/LoadingSmall',
  component: LoadingSmallView,
} as Meta;

export const Default = () => <LoadingSmallView />;
